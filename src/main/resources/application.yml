spring:
  application:
    name: sicredi-voting
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false



server:
  port: 8080